<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RunnersHi" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme: 아이콘 없이 배경색만 표시 -->
    <style name="Theme.RunnersHi.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.RunnersHi</item>
    </style>
</resources>
